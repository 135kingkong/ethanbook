
<div class="page-header">
	<h1>Status</h1>
</div>

<div class="status">   
	<strong><%= @status.user.full_name %></strong>
    <p><%= @status.content %></p>
	<div class="meta">
		<%= link_to 'Back to Wall', root_path %> | 
        <%= link_to time_ago_in_words(@status.created_at) + " ago", @status %>
		<span class="admin">
        	| <%= link_to 'Edit', edit_status_path(@status) %>
        	
		</span>
	</div>
</div>     